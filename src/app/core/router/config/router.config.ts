import { CustomRouter } from '../models/router.model';

export const routerConfig: CustomRouter = {
  register: 'register',
  login: 'login',
  dashboard: 'dashboard',
  products: 'products',
  cart: 'cart',
  home: '',
};
